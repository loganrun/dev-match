

<div class= "row">
  <div class="col-md-6 col-md-offset-3 text-center">
  <% if params[:plan] == '2' %>
    <h1>Pro Account</h1>
    <h4>Sign up for the pro account and get full access to the community and contact information!  Only $10.00 a month!</h4>
  <% else %>
    <h1>Basic Account</h1>
    <h4>Sign up for free and get basic access to our community</h4>
  <% end %>
  </div>
  
  <% if params[:plan] == '2' %>
      <%= render "pro_form" %>
      <% else %>
          <%= render "basic_form" %>
  <% end %>

          <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>